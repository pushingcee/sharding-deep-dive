# Sharded (hash-routing) profile configuration.
# DataSources are configured manually in ManualShardingDataSourceConfig.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
