# Lookup-table sharding profile configuration.
# DataSources are configured manually in LookupTableShardingDataSourceConfig.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
